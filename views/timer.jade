extends layout

block content
  #timer
  script
    :coffee
      socket.on 'connect', (data) ->
        socket.emit 'timer'
      socket.on 'countdown', (data) ->
        console.log(data)
        $('#timer').text(data.display)
      socket.on 'blah', (data) ->
        $('#timer').text('blah')
