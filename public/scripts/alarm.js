// Generated by CoffeeScript 1.6.3
(function() {
  var alarmTime, intervalToTime, moment, multiplyTime;

  moment = require('moment');

  module.exports = function(socket, alarmTime, goBack) {
    var ringInterval;
    console.log('alarm called');
    return ringInterval = [1, 0.38, 0.618421052631579, 0.624113475177305, 0.613636363636364, 0.62962962962963, 0.617647058823529, 0.619047619047619, 0.615384615384615, 0.625, 0];
  };

  alarmTime = moment(this.alarmTime);

  console.log(alarmTime);

  multiplyTime = function(time, multiplier, scale) {
    if (scale == null) {
      scale = seconds;
    }
    return moment(time * moment.duration(multiplier, scale));
  };

  intervalToTime = function(alarmTime, ringInterval) {
    var i, minuteInterval;
    this.ringInterval = ringInterval;
    minuteInterval = [alarmTime - moment.duration(-this.goBack, "minutes")];
    minuteInterval.push((function() {
      var _i, _len, _ref, _results;
      _ref = this.ringInterval;
      _results = [];
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        i = _ref[_i];
        _results.push(multiplyTime(mintuteInterval.slice(-1), i, "minutes"));
      }
      return _results;
    }).call(this));
    return console.log(minuteInterval);
  };

  console.log(multiplyTime(moment(), .38, "minutes"));

}).call(this);
