// Generated by CoffeeScript 1.6.3
(function() {
  var wh;

  wh = window.wholeHouse = {};

  wh.tools = {};

  wh.tools.countdown = function(milliseconds, update, complete) {
    var callback, frequency, start;
    if (complete == null) {
      complete = null;
    }
    start = new Date;
    frequency = 100;
    callback = function() {
      var now, timeLeft;
      now = new Date;
      timeLeft = new Date((milliseconds + 1000) - (now - start));
      update(timeLeft);
      if (now - start < milliseconds) {
        return setTimeout(callback, frequency);
      } else if (complete) {
        return complete();
      }
    };
    return setTimeout(callback, frequency);
  };

  wh.tools.padLeft = function(length, string, padString) {
    var i, _i, _ref;
    string = string.toString();
    if (string.length < length) {
      for (i = _i = 1, _ref = length - string.length; 1 <= _ref ? _i <= _ref : _i >= _ref; i = 1 <= _ref ? ++_i : --_i) {
        string = padString + string;
      }
    }
    return string;
  };

  wh.tools.randrange = function(min, max) {
    return Math.floor(Math.random() * ((max - min) + min));
  };

}).call(this);
