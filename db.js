// Generated by CoffeeScript 1.6.3
(function() {
  var db, dbConfig, orientdb, server, serverConfig;

  orientdb = require('orientdb');

  dbConfig = {
    user_name: 'admin',
    user_password: 'admin'
  };

  serverConfig = {
    host: '24.75.121.111',
    port: 2480
  };

  server = new orientdb.Server(serverConfig);

  db = new orientdb.GraphDb('wholeHouse', server, dbConfig);

  db.open(function(err) {
    if (err) {
      throw err;
    }
    return console.log('Connected to OrientDB');
  });

}).call(this);
